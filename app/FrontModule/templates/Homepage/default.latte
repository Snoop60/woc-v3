{block content}

    {foreach $posts as $post}
<div id="blog"></div>
	<div class="blog">
	    <h2>{$post['title']}</h2>
		<p>{$post['body']|truncate:300}</p>
		<p>{$post['date']|date}</p>
		<a n:href="Homepage:show $post['id']">vice</a>
		{/foreach} 
		</div>
    </div>
{/block}